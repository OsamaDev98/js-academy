import Link from 'next/link'; import {ArrowLeft,BookOpen,Code2,Layers3,Zap} from 'lucide-react'; import {lessons} from '@/lib/lessons';
export default function Home(){const cats=['JavaScript','Async JavaScript','Node.js'];return <main className="shell"><header className="top"><Link href="/" className="brand"><b>JS</b> Academy</Link><nav><a href="#courses">الدروس</a><a href="#about">عن المنهج</a></nav></header><section className="hero"><div><span className="eyebrow">JAVASCRIPT → ASYNC → NODE.JS</span><h1>ذاكر JavaScript وNode.js <em>بطريقة احترافية.</em></h1><p>شرح عربي بسيط، أمثلة كود، Output، ونقاط مهمة تساعدك تفهم بدل ما تحفظ.</p><div className="actions"><Link className="primary" href={`/learn/${lessons[0].slug}`}>ابدأ المذاكرة <ArrowLeft size={18}/></Link><a className="ghost" href="#courses">استكشف المنهج</a></div></div><div className="hero-card"><div className="code"><span>● ● ●</span><pre>{`const server = createServer();\n\nserver.listen(3000, () => {\n  console.log("Ready 🚀");\n});`}</pre></div><div className="stats"><b>{lessons.length}+</b> درس منظم <b>RTL</b> ومتجاوب</div></div></section><section id="courses" className="section"><div className="section-head"><div><span className="eyebrow">LEARNING PATH</span><h2>مسار المذاكرة</h2></div><small>{lessons.length} درس</small></div><div className="grid">{cats.map((cat,i)=>{const list=lessons.filter(x=>x.category===cat);const Icon=[Code2,Zap,Layers3][i];return <article className="card" key={cat}><div className="icon"><Icon/></div><h3>{cat}</h3><p>{list.length} دروس — شرح + أمثلة عملية</p>{list.map((l,n)=><Link href={`/learn/${l.slug}`} key={l.slug}><span>{String(n+1).padStart(2,'0')}</span>{l.title}<ArrowLeft size={14}/></Link>)}</article>})}</div></section><section id="about" className="features"><div><BookOpen/><h3>افهم قبل ما تحفظ</h3><p>كل درس يبدأ بالفكرة ثم يوضحها بمثال عملي.</p></div><div><Code2/><h3>كود قابل للتجربة</h3><p>أمثلة قصيرة مناسبة للمذاكرة والمراجعة.</p></div><div><Zap/><h3>تدرج منطقي</h3><p>من JavaScript إلى Async ثم Node.js.</p></div></section><footer>JS Academy · Built for focused learning</footer></main>}
